// Example parameter file for DLBC
//
// Empty lines are ignored, as is anything beyond '//' on a line.
// Multiline comments are *not* supported.
// Section headers (corresponding to modules) are enclosed in square brackets.

[parallel]
ncy = 2

[lattice]
gnx = 32    // Lattice size x-direction
gny = 32    // Lattice size y-direction
gnz = 32    // Lattice size z-direction

[random]
seed = 42

[logging]
showTime = true

