// Example parameter file for DLBC
//
// Empty lines are ignored, as is anything beyond '//' on a line.
// Multiline comments are *not* supported.
// Section headers (corresponding to modules) are enclosed in square brackets.

[io]
simulationName = foobar
outputFormat = HDF5
outputPath = output
createPath = true

startOutput = 0
velFreq = 100
fluidsFreq = 100
forceFreq = 100
profileFreq = 0

checkpoint.cpFreq = 100
checkpoint.cpPath = cp

hdf5.writeChunked = false

[lb]
timesteps = 1000
components = 2
fieldNames = [ "red", "blue" ]

force.gcc = [ 0.0, 0.2, 0.2, 0.0 ]
force.enableShanChen = true
force.globalAcc = [ 0.0, 0.0, 0.0 ]

init.fluidInit = [ EqDistRandom, EqDistRandom ]
init.fluidDensity = [0.9, 0.3 ]

mask.maskInit = None
mask.maskFile = mask.h5

[parallel]
showTopology = false
nc = [ 0, 0, 0 ]

[lattice]
gn = [32, 32, 32 ]    // Lattice size

[random]
seed = 42

